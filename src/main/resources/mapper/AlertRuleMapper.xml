<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 告警规则 Mapper：管理股票监控告警规则的增删改查，包含阈值、条件表达式、通知渠道等配置 -->
<mapper namespace="io.leavesfly.alphaforge.infrastructure.persistence.alert.AlertRuleRepository">

    <!-- 结果映射：数据库字段 ↔ AlertRule 实体属性 -->
    <resultMap id="alertRuleMap" type="io.leavesfly.alphaforge.domain.model.entity.alert.AlertRule">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="stockCode" column="stock_code"/>
        <result property="stockName" column="stock_name"/>
        <result property="alertType" column="alert_type"/>
        <result property="targetScope" column="target_scope"/>
        <result property="target" column="target"/>
        <result property="severity" column="severity"/>
        <result property="thresholdValue" column="threshold_value"/>
        <result property="conditionExpr" column="condition_expr"/>
        <result property="parameters" column="parameters"/>
        <result property="enabled" column="enabled"/>
        <result property="triggered" column="triggered"/>
        <result property="lastTriggeredAt" column="last_triggered_at"/>
        <result property="oneShot" column="one_shot"/>
        <result property="notifyChannels" column="notify_channels"/>
        <result property="source" column="source"/>
        <result property="note" column="note"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <!-- 插入一条告警规则 -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO alert_rules (name, stock_code, stock_name, alert_type, target_scope, target, severity, threshold_value, condition_expr, parameters, enabled, triggered, last_triggered_at, one_shot, notify_channels, source, note, created_at, updated_at)
        VALUES (#{name}, #{stockCode}, #{stockName}, #{alertType}, #{targetScope}, #{target}, #{severity}, #{thresholdValue}, #{conditionExpr}, #{parameters}, #{enabled}, #{triggered}, #{lastTriggeredAt}, #{oneShot}, #{notifyChannels}, #{source}, #{note}, #{createdAt}, #{updatedAt})
    </insert>

    <!-- 更新告警规则 -->
    <update id="update">
        UPDATE alert_rules SET name=#{name}, stock_code=#{stockCode}, stock_name=#{stockName}, alert_type=#{alertType}, target_scope=#{targetScope}, target=#{target}, severity=#{severity}, threshold_value=#{thresholdValue}, condition_expr=#{conditionExpr}, parameters=#{parameters}, enabled=#{enabled}, triggered=#{triggered}, last_triggered_at=#{lastTriggeredAt}, one_shot=#{oneShot}, notify_channels=#{notifyChannels}, source=#{source}, note=#{note}, updated_at=#{updatedAt}
        WHERE id=#{id}
    </update>

    <!-- 根据主键 ID 查询告警规则 -->
    <select id="findById" resultMap="alertRuleMap">
        SELECT * FROM alert_rules WHERE id = #{id}
    </select>

    <!-- 查询全部告警规则，按创建时间倒序 -->
    <select id="findAll" resultMap="alertRuleMap">
        SELECT * FROM alert_rules ORDER BY created_at DESC
    </select>

    <!-- 根据主键 ID 删除告警规则 -->
    <delete id="deleteById">
        DELETE FROM alert_rules WHERE id = #{id}
    </delete>

    <!-- 根据股票代码查询已启用的告警规则 -->
    <select id="findByStockCodeAndEnabledTrue" resultMap="alertRuleMap">
        SELECT * FROM alert_rules WHERE stock_code = #{stockCode} AND enabled = 1
    </select>

    <!-- 查询所有已启用的告警规则，按创建时间倒序 -->
    <select id="findByEnabledTrueOrderByCreatedAtDesc" resultMap="alertRuleMap">
        SELECT * FROM alert_rules WHERE enabled = 1 ORDER BY created_at DESC
    </select>

    <!-- 根据告警类型查询规则，按创建时间倒序 -->
    <select id="findByAlertTypeOrderByCreatedAtDesc" resultMap="alertRuleMap">
        SELECT * FROM alert_rules WHERE alert_type = #{alertType} ORDER BY created_at DESC
    </select>

    <!-- 查询已启用但尚未触发的告警规则（用于定时扫描触发） -->
    <select id="findByTriggeredFalseAndEnabledTrue" resultMap="alertRuleMap">
        SELECT * FROM alert_rules WHERE triggered = 0 AND enabled = 1
    </select>

    <!-- 按启用状态查询告警规则 -->
    <select id="findByEnabled" resultMap="alertRuleMap">
        SELECT * FROM alert_rules WHERE enabled = #{enabled}
    </select>

    <!-- 统计已启用的告警规则数量 -->
    <select id="countByEnabledTrue" resultType="long">
        SELECT COUNT(*) FROM alert_rules WHERE enabled = 1
    </select>

</mapper>
