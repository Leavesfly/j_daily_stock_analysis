<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 分析报告 Mapper：管理股票智能分析报告的存储与查询，包含评分、信号、技术面/基本面/新闻分析等 -->
<mapper namespace="io.leavesfly.alphaforge.domain.repository.analysis.AnalysisReportRepository">

    <!-- 结果映射：数据库字段 ↔ AnalysisReport 实体属性 -->
    <resultMap id="analysisReportMap" type="io.leavesfly.alphaforge.domain.model.entity.analysis.AnalysisReport">
        <id property="id" column="id"/>
        <result property="stockCode" column="stock_code"/>
        <result property="stockName" column="stock_name"/>
        <result property="analysisDate" column="analysis_date"/>
        <result property="market" column="market"/>
        <result property="currentPrice" column="current_price"/>
        <result property="changePct" column="change_pct"/>
        <result property="totalScore" column="total_score"/>
        <result property="signal" column="signal"/>
        <result property="confidence" column="confidence"/>
        <result property="summary" column="summary"/>
        <result property="technicalAnalysis" column="technical_analysis"/>
        <result property="fundamentalAnalysis" column="fundamental_analysis"/>
        <result property="newsAnalysis" column="news_analysis"/>
        <result property="fullReport" column="full_report"/>
        <result property="llmResponse" column="llm_response"/>
        <result property="agentMode" column="agent_mode"/>
        <result property="llmModel" column="llm_model"/>
        <result property="durationSeconds" column="duration_seconds"/>
        <result property="tokenUsage" column="token_usage"/>
        <result property="isDryRun" column="is_dry_run"/>
        <result property="reportLanguage" column="report_language"/>
        <result property="skills" column="skills"/>
        <result property="analysisPhase" column="analysis_phase"/>
        <result property="selectionSource" column="selection_source"/>
        <result property="taskId" column="task_id"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <!-- 插入一条分析报告 -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO analysis_report (stock_code, stock_name, analysis_date, market, current_price, change_pct, total_score, signal, confidence, summary, technical_analysis, fundamental_analysis, news_analysis, full_report, llm_response, agent_mode, llm_model, duration_seconds, token_usage, is_dry_run, report_language, skills, analysis_phase, selection_source, task_id, created_at)
        VALUES (#{stockCode}, #{stockName}, #{analysisDate}, #{market}, #{currentPrice}, #{changePct}, #{totalScore}, #{signal}, #{confidence}, #{summary}, #{technicalAnalysis}, #{fundamentalAnalysis}, #{newsAnalysis}, #{fullReport}, #{llmResponse}, #{agentMode}, #{llmModel}, #{durationSeconds}, #{tokenUsage}, #{isDryRun}, #{reportLanguage}, #{skills}, #{analysisPhase}, #{selectionSource}, #{taskId}, #{createdAt})
    </insert>

    <!-- 更新分析报告 -->
    <update id="update">
        UPDATE analysis_report SET stock_code=#{stockCode}, stock_name=#{stockName}, analysis_date=#{analysisDate}, market=#{market}, current_price=#{currentPrice}, change_pct=#{changePct}, total_score=#{totalScore}, signal=#{signal}, confidence=#{confidence}, summary=#{summary}, technical_analysis=#{technicalAnalysis}, fundamental_analysis=#{fundamentalAnalysis}, news_analysis=#{newsAnalysis}, full_report=#{fullReport}, llm_response=#{llmResponse}, agent_mode=#{agentMode}, llm_model=#{llmModel}, duration_seconds=#{durationSeconds}, token_usage=#{tokenUsage}, is_dry_run=#{isDryRun}, report_language=#{reportLanguage}, skills=#{skills}, analysis_phase=#{analysisPhase}, selection_source=#{selectionSource}, task_id=#{taskId}
        WHERE id=#{id}
    </update>

    <!-- 根据主键 ID 查询分析报告 -->
    <select id="findById" resultMap="analysisReportMap">
        SELECT * FROM analysis_report WHERE id = #{id}
    </select>

    <!-- 查询全部分析报告 -->
    <select id="findAll" resultMap="analysisReportMap">
        SELECT * FROM analysis_report
    </select>

    <!-- 根据主键 ID 删除分析报告 -->
    <delete id="deleteById">
        DELETE FROM analysis_report WHERE id = #{id}
    </delete>

    <!-- 根据股票代码查询分析报告，按分析日期倒序 -->
    <select id="findByStockCodeOrderByAnalysisDateDesc" resultMap="analysisReportMap">
        SELECT * FROM analysis_report WHERE stock_code = #{stockCode} ORDER BY analysis_date DESC
    </select>

    <!-- 查询全部分析报告，按分析日期倒序 -->
    <select id="findAllByOrderByAnalysisDateDesc" resultMap="analysisReportMap">
        SELECT * FROM analysis_report ORDER BY analysis_date DESC
    </select>

    <!-- 按日期范围查询分析报告，按分析日期倒序 -->
    <select id="findByAnalysisDateBetweenOrderByAnalysisDateDesc" resultMap="analysisReportMap">
        SELECT * FROM analysis_report WHERE analysis_date BETWEEN #{start} AND #{end} ORDER BY analysis_date DESC
    </select>

    <!-- 按市场查询分析报告，按分析日期倒序 -->
    <select id="findByMarketOrderByAnalysisDateDesc" resultMap="analysisReportMap">
        SELECT * FROM analysis_report WHERE market = #{market} ORDER BY analysis_date DESC
    </select>

    <!-- 按信号类型查询分析报告，按分析日期倒序 -->
    <select id="findBySignalOrderByAnalysisDateDesc" resultMap="analysisReportMap">
        SELECT * FROM analysis_report WHERE signal = #{signal} ORDER BY analysis_date DESC
    </select>

    <!-- 统计某只股票的分析报告数量 -->
    <select id="countByStockCode" resultType="long">
        SELECT COUNT(*) FROM analysis_report WHERE stock_code = #{stockCode}
    </select>

    <!-- 统计分析报告总数 -->
    <select id="count" resultType="long">
        SELECT COUNT(*) FROM analysis_report
    </select>

</mapper>
